# Web framework
fastapi
uvicorn[standard]

# Data validation
pydantic

# Environment
python-dotenv

# HTTP client
httpx

# AI / ML
openai
google-generativeai

# Image processing
pillow
opencv-python-headless
mediapipe==0.10.9
protobuf==3.20.3

# Document generation
reportlab

# File upload
python-multipart

# Cloud storage
azure-storage-blob

# Async MongoDB driver (optional — used only if MONGO_URL is set)
# If not installed, MongoSessionStore is unavailable but AzureTableSessionStore
# and NullSessionStore work without it.
motor

# Azure Table Storage — session index for image quality evaluator
# Uses the same Storage Account as blob storage (no extra subscription needed).
azure-data-tables

# Auth — password hashing and JWT session tokens
passlib[bcrypt]
PyJWT
